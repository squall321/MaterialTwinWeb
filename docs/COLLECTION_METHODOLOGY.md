# MaterialTwin 물성 수집 방법론

**빈칸을 어떻게 찾아내고, 어떻게 채우고, 어떻게 검증하고, 어떻게 더 믿을 만한 출처로 갈아 끼우는가.**

이 문서는 규칙집이 아니라 **열세 번의 파동을 돌리며 실제로 깨지고 고쳐진 프로세스의 기록**이다.
처음 보는 사람이 이 문서만으로 같은 사이클을 재현할 수 있게 쓴다.
읽는 사람은 셋을 가정한다 — 해석 입력을 받아 쓰는 **솔버팀**, 값을 채우는 **물성팀**,
"지금 몇 %이고 남은 일이 얼마인가"를 묻는 **경영진**.

- 수치 기준일 — **2026-08-10**. 전부 라이브 DB에서 직접 뽑았다.
- DB — `/home/koopark/claude/HEAXHub/var/app_data/materialtwin_web/materialtwin.db`
- 재현 — `python3 backend/scripts/catalog/coverage_report.py`, `python3 backend/scripts/catalog/integrity_check.py`
- 경위의 1차 사료 — `docs/PROPERTY_DATA_HISTORY.md`(76개 장), 지시서 정본 — `docs/COLLECTION_BRIEF.md`

---

## 1. 문제 정의 — 왜 물성 카탈로그인가

### 1.1 해석이 도는 것과 안 도는 것

해석 하나를 돌리려면 그 해석이 요구하는 물성이 **전부** 있어야 한다.
아홉 개가 있고 하나가 없으면 결과는 90%가 아니라 **0%**다. 덱이 안 만들어진다.

이 성질이 이 프로젝트의 모든 판단을 지배한다.

- 물성값 총량을 늘리는 것과 해석 가능한 재료를 늘리는 것은 **다른 일**이다.
- 그래서 "물성값 21,279건"은 성과 지표가 아니다. **재료마다 마지막 한 칸이 다르면
  총량이 아무리 늘어도 돌아가는 해석은 늘지 않는다.**
- 8차 파동에서 방침이 한 번 크게 바뀐 이유도 이것이다 —
  **덱이 도는 것과 안 도는 것의 차이가, 그 값이 tier1이냐 tier3이냐의 차이보다 크다.**

### 1.2 격자 — 무엇을 세고 있나

카탈로그는 **재료 544종 × 물성 정의 161종**이다. 그러나 이 8만 7천 칸을 다 세지 않는다.
알루미나에 투습도를 요구하거나 벌크 금속에 층두께를 요구하면 **없는 결함을 만들어내는
지표**가 된다(실제로 그렇게 세다가 벤딩을 8%, 결로를 4%로 잘못 셌다 — 21장).

대신 **해석 13종이 실제로 요구하는 (재료 × 필수물성) 격자 14,478칸**을 센다.

| 해석 | 대상 | 필수물성 | 택일군(하나만 있어도 됨) | 칸 |
|---|---:|---|---|---:|
| 구조·강성 | 544 | E, ν, ρ | — | 1,632 |
| 낙하·충격 | 544 | E, ν, ρ | 소성(항복 OR 인장+연신) · 율속(C-S · DIF · σy(ε̇) · JC-C) | 2,720 |
| 열전달·방열 | 544 | ρ, k, cp | — | 1,632 |
| 열응력·워피지 | 544 | E, ν, ρ, CTE | — | 2,176 |
| 폴더블 벤딩 | 185 | E, ν, ρ | Prony(τ·E·G·상대계수) OR G | 740 |
| 결로(표면) | 544 | ρ, k, cp | 접촉각 OR 표면에너지 | 2,176 |
| 결로(투습) | 417 | ρ, k, cp | WVTR · 투습계수 · 확산계수 · 흡습 3종 | 1,668 |
| 초탄성(실링·완충) | 46 | ρ | 초탄성 계수 OR 지수 | 92 |
| 접착·박리 | 185 | ρ | peel · lap shear · die shear | 370 |
| 전기·EMI | 544 | — | 유전율 · 체적저항률 · 손실계수 | 544 |
| 피로·수명 | 198 | E, ρ | Basquin · Coffin-Manson · Darveaux · Morrow · Stromeyer | 594 |
| 산소소광-발광체 | 55 | — | 여기수명 · Stern-Volmer · 이분자소광속도 | 55 |
| 산소확산-매트릭스 | 79 | — | O₂ 투과계수 · 용해도 | 79 |
| **합계** | | | | **14,478** |

**택일군이 있는 이유** — 소성·파괴 입력은 항복강도로도 되고 인장강도+파단연신율로도 된다.
GF 강화 등급은 항복점을 애초에 재지 않는 것이 정상인데 이걸 결측으로 세는 바람에
**146종이 부당하게 미달로 잡혀** 낙하 준비율이 22%로 찍힌 적이 있다(58장). 고치니 74%였다.

**대상 필터가 있는 이유** — 산소 소광을 "발광체"와 "매트릭스" 한 묶음으로 재던 시절
92종 중 71종이 **원리적으로 못 채우는 칸**을 세고 있었다. 0.0%는 수집 실패가 아니라
척도 오류였다(66장).

---

## 2. tier 체계 — 등급이 무엇을 뜻하고 왜 tier4를 허용하는가

### 2.1 네 등급

tier의 기준은 **"문서의 종류"가 아니라 "값의 성격"**이다. 벤더 TDS든 논문이든 상관없다.

| tier | 뜻 | 건수 | 비율 |
|---|---|---:|---:|
| **1** | 그 제품·시편에 대해 **문서에 인쇄된 실제 시험값**(벤더 TDS·그 재료를 시험한 논문) | 9,535 | 44.8% |
| **2** | 핸드북·표준 최소보증치·공인 DB(MMPDS·ASM·NIST·CDA·ISO/ASTM) | 1,640 | 7.7% |
| **3** | 계열·클래스 대표값 / 2차 인용 / 등급 불일치 대체값 / 그래프 좌표 추출 | 5,614 | 26.4% |
| **4** | 계산·유도·추정·가정 | 4,490 | 21.1% |
| | **합계** | **21,279** | |

`method`는 **measured / handbook / computed / estimated** 넷만 쓴다.
`datasheet`는 방법이 아니라 출처 종류다(한때 method로 쓰여 902건이 섞여 있었다).

### 2.2 tier4를 허용하되 반드시 표시하는 이유

7차까지는 "빈 칸이 틀린 값보다 낫다"를 가장 강하게 해석했다.
8차에서 방침이 바뀌었다 — **계열 대표값(tier3)과 근거 있는 가정(tier4)까지 허용해
일단 칸을 닫는다.** 다만 바뀐 것은 **어느 tier까지 허용하느냐**이지
**근거 없이 지어내도 되느냐**가 아니다.

그래서 tier4에는 **두 개의 구조적 표지**를 필수로 박는다.

```json
"conditions": { "assumption": true },
"method": "estimated"
```

이 두 줄이 하는 일은 셋이다.

| 장치 | 어디서 작동하나 | 효과 |
|---|---|---|
| 대표값 선택(`_rep_rank`) | 조회·비교·DYNA 카드 | tier가 낮은 값이 이긴다 → 가정값은 **항상 실측에 밀린다** |
| UI 배지(`tierBadge()`) | 화면 | tier4 + `assumption`이면 "계산/등가"가 아니라 **"가정"**(danger 색)으로 표시 |
| 무결성 검사 2항목 | 배포 전 | `가정값인데 tier4·estimated 아님` · `가정값인데 근거 출처 없음` |

**핵심은 자동 대체다.** 나중에 실측이 들어오면 그 순간 대표값이 바뀐다 —
사람이 손으로 지울 필요가 없다.

**실제로 작동한 것을 확인한 사례가 있다.**

- 배터리 분리막 포아송비가 **tier4 추정 0.4 → tier2 실측 0.17**로 대표값이 갈아 끼워졌다(47장).
  가정값을 넣을 때 설계한 대로 동작했다.
- Al7050-T7451의 피로계수가 **tier4 추정(Meggiolaro-Castro 1.9×Su = 996 MPa) →
  tier3 실측 계열대체(AA7075-T6 FD&E 피팅 876 MPa)**로 올라갔다(FDE_FATIGUE_ARCHIVE).

### 2.3 그리고 반대 방향의 함정

가정값이 정직하게 새겨져 있어도 **가정 위에 가정을 쌓으면** 소용없다.
포아송비 가정값을 만들 때 처음엔 전체 값으로 클래스 중앙값을 냈는데,
폼 4건이 전부 0.300이고 편차가 0이라 이상해서 보니 **이미 tier4 가정값**이었다(33장).
근거를 tier1·2로 한정해 다시 계산했다.

**앵커로 쓸 행은 값과 tier만 보면 안 된다. `method`와 `notes`를 반드시 읽어야 한다.**
8차 파동이 VHB 계열 13종에 0.894 MPa를 옮기면서 근거를 "Satas 핸드북 계열값"이라 적었는데
실제 정체는 같은 재료의 G와 ν로 만든 **역산값**이었다(71장).

---

## 3. 절대 넘지 않는 선 — 그리고 그 규칙이 막아 온 사고

규칙은 추상이 아니다. **전부 실제로 사고가 나서 생겼다.**

### 규칙 1 — 지어내지 않는다. 빈 칸이 틀린 값보다 낫다.

문서에 실제로 인쇄된 숫자만 넣는다.

- **라미네이트 밀도 16종을 비워 뒀다.** Isola 370HR SDS가
  `SPECIFIC GRAVITY: Not Available for product`라고 **명기**한다. 유사 제품 값(RF-35TC 2.35 등)을
  규칙대로 대입하지 않았다 — **이 칸은 비어 있는 게 정답이다.**
- **Kaneka Graphinity의 1200 W/mK를 채택하지 않았다.** 무역기사에만 뜨는 숫자이고
  데이터시트도 논문도 아니다. 제품 페이지는 404이고 Wayback 스냅샷도 없다.
- **8차 파동은 항복강도 258종 중 69종만 닫았고 그게 정답이었다.** 171종은 항복강도가
  **물리적으로 존재하지 않는다** — 세라믹·유리는 취성 파괴(49종), 고무는 초탄성(64종),
  저분자 유기 고체는 벌크 소성 시험이 성립하지 않는다(55종).

### 규칙 2 — 역산 금지. 다른 물성에서 계산한 값을 저장하지 않는다.

가정과 역산은 다르다. **가정은 "이 계열은 대개 이 값이다"이고, 역산은 "다른 물성에서 계산했다"이다.**

| 사고 | 실체 | 피해 |
|---|---|---|
| VHB 영률 0.894 MPa (71장) | `E = 2G(1+ν)` 역산값인데 notes에는 "Satas 핸드북 계열값" | **역산값 하나가 13종으로 번지고 근거 표기까지 틀린 채로 남았다.** 14행을 되돌렸다 |
| AZoM/Granta의 폴리머 `Shear Modulus` (69장) | PU·PET·PP·PI·에폭시 5계열에서 `E/(2(1+ν))`와 **유효숫자까지 일치** | 폴더블 벤딩 G 칸을 채워주는 최대 출처가 통째로 쓸 수 없는 값이었다 |
| MakeItFrom의 폴리머 G (69장) | LDPE가 E=0.30 GPa·G=0.21 GPa를 동시에 인쇄 → **ν=−0.29** | 같은 사이트의 **금속**은 정합한다. 폴리머만 오염 |
| Nitto 5000 박리강도 12.5 MPa (69장) | 원문 "20 N/cm"(폭당 힘)를 누군가 두께로 나눔. **tier1 실측으로 들어가 있었다** | 테이프는 점착층+캐리어 적층이라 전체 두께로 나눈 값은 **어느 층의 강도도 아니다** |
| Panasonic PGS·Kaneka 흑연시트 (72장) | 도전율(S/cm)만 인쇄된 것을 역수 취해 저항률로 넣으려 함 | 3종을 비웠다 |

**단, 9차 파동에서 한 칸을 열었다(0-B절).**
**문헌이 "실측이 없을 때 이렇게 추정하라"고 공표한 상관식은 쓴다** —
Universal Slopes(Manson 1965), Modified Universal Slopes(1988), Roessle-Fatemi(2000),
Meggiolaro-Castro Medians(2004), Gent(1958). 출처는 **그 추정식 논문**이고 입력값과 식을
notes에 전부 적는다.

**둘의 차이는 누가 그 관계를 보증하느냐다.** 공표된 상관식은 저자가 적용 범위와 오차까지
밝히고, 손으로 세운 항등식은 아무도 보증하지 않는다.

### 규칙 3 — 그래프에서 값을 읽지 않는다.

- **아크릴 PSA 열전도율 앵커 0.176 W/(m·K)** — notes는 "0 wt% 대조군 실측"이라 적혀 있었으나
  **인용 논문 전문에 `0.176`은 0회 등장한다.** 논문이 인쇄한 것은 충전계 0.8347과 0.9552뿐이고
  순수 PSA 값은 Figure에만 있다. 3M 467MP TDS 인쇄값 0.158로 교체했다(71장).
- **SUS430 율속 데이터 전량 폐기** — 원문 터키어가 "그림 4에서 읽었다"였다(69장).
- **예외 하나** — `pdftotext`가 조용히 빈 표를 주면 그 표는 **이미지**다.
  `pdftoppm -png`로 렌더해 읽는 것은 **그래프가 아니라 표이므로 규칙 3에 걸리지 않는다.**
  이 단계가 없었으면 7차 파동이 표 전체를 "없다"고 결론지었다.

### 규칙 4 — 재인쇄본·2차 인용을 믿지 않는다.

"[12]에 따르면"으로 시작하는 표는 1차 출처까지 따라간다. 못 가면 tier3으로 낮추고 notes에 적는다.

- **Polymers 12(10) 2327 Table 4에서 온 19건** — 캡션이 `[48,56-67]`로 2차 인용을 밝히고,
  같은 표의 PC 밀도 1.44·PE 1.38이 실제값(1.20·0.92)과 어긋난다. 지우지 않고
  **각 행에 검산 실패를 박았다**(69장).
- **Kwatra Table 2.1이 Ferguson 값을 8.18로 오기** — 원문은 8.48이다. 2차 블록을 폐기하고
  1차로 교체(51장).
- **복사 계보** — PV 적층 물성표(EVA 2090, Tedlar 1250)가 arXiv·Buildings·PMC
  **세 문헌에 바이트 단위로 복사**돼 있었다. **같은 숫자 조합이 여러 논문에 나오면
  독립 확인이 아니라 한 출처의 재인쇄다.** 한 건을 검산해 버리면 나머지도 같이 버린다(71장).

### 규칙 5 — 조건 없는 값은 값이 아니다.

온도·주파수·방향·습도·하중비·파장은 그 물성의 **정의 조건**이다.

- **3M VHB 4910 TDS의 `Shear Modulus 6 × 10⁵ Pa`** — 시험규격도 변형률속도도 없다.
  같은 재료의 율속 분해 문헌값(평형 1.726e4 / 순간 4.876e4 Pa)보다 12~35배 크다.
  tier1로 두면 **조건 없는 값이 대표로 뽑혀 조건 붙은 값을 밀어낸다.** tier3으로 내렸다(24장).
- **파장 없는 굴절률·반사율 75건** — 파장이 없으면 값 자체가 해석 불가다.
  실리콘 소광계수는 400 nm에서 0.387, 1000 nm에서 0.0005로 **800배** 차이난다.
  tier4로 내려 대표값에서 빠지게 했다(지우지는 않았다).
- **필수 조건 목록** — 열전도율·CTE·유전율은 온도, 복합재·박막은 방향 / 유전율·손실계수는
  주파수 / WVTR은 온도·RH·두께 / 접촉각은 액체 종류와 전진·후퇴·정적 / 박리강도는
  각도·피착재·속도.

**충돌은 값의 문제가 아니라 조건이 덜 적힌 문제인 경우가 많다.**
인제스터가 "조건이 같아 뒤엣것이 앞엣것을 덮는다"고 5건을 거부한 적이 있는데, 뜯어보니
Nitinol은 **초탄성이라 항복점이 둘**(상변태 개시 / 전위 소성)이었고 Al6063은
**같은 공칭 율속의 다른 시편**이었다. `yield_definition`·`specimen`을 조건에 넣자
76건 전부 들어갔다(69장).

### 규칙 6 — 그 재료의 상위 tier 값과 자릿수가 어긋나면 넣지 않는다.

**등급 불일치는 tier를 낮춰 쓸 수 있지만, 같은 재료의 tier1 실측과 직접 모순되는 값은 다르다.**
전자는 절대값을 못 믿어도 배율이나 형상이 쓸모 있다.
후자는 **카드를 물리적으로 앞뒤 안 맞게 만든다.**

| 버린 것 | 무엇과 모순인가 |
|---|---|
| `Foam PU Impact Absorber` 5항 Prony (64·65장) | 수집은 흠잡을 데 없었으나 논문 시료가 65 kg/m³이고 카탈로그의 그 재료는 **tier1 밀도 240 kg/m³**. 순간계수 76 kPa가 그 재료의 가장 무른 tier1 영률보다도 3배 무르다 |
| 전해동박 항복강도 12행 (69장) | 항복 467~600 MPa인데 그 재료의 **tier1 인장강도가 147~276 MPa**. 항복이 자기 UTS의 2배 |
| Norseal F-12 초탄성 4행 (68장) | 피팅 Σμ=0.557 MPa → E≈1.1 MPa인데 tier1 TDS 압축변형응력은 20.7 kPa. 13배 |
| UCF 학위논문의 Kapton 25 µm 5항 Prony (66장) | "PI 필름 Prony는 없다"는 벽을 뚫은 값인데도 버렸다. 그 세트대로면 25~60 °C에서 1시간에 41% 완화하는데 Tg 360 °C대 PI에서 불가능 |
| CEL-8240HF10GK 굽힘탄성률 (69장) | CEL-9240HF에 대입하려다 같은 재료의 **tier1 Prony(글래시 G 합 → E≈18 GPa)와 44% 어긋나** 폐기 |

**반대로 살린 것도 있다.** VHB 4991의 초탄성은 tier1 준정적 값과 규모가 달라도 남겼다 —
그건 Prony와 짝지어 쓰는 **순간(glassy) 가지**라 모순이 아니다.
**자릿수 차이가 언제 모순이고 언제 아닌지를 모델이 정한다.**

### 규칙 7 — 인접 열 오독을 조심한다.

이 유형 사고가 **누적 20건 넘게** 잡혔다. 값을 뽑을 때 그 값이 있는 **열 머리글을 다시 읽는다.**
PDF 열이 섞여 보이면 `pdftotext -bbox`로 토큰 x좌표를 직접 확인한다.

실제 사례 — `Spezifische Wärmeleitfähigkeit`(열전도율)를 비열로 · (BH)max를 굽힘강도로 ·
`Specific Gravity 1.07`을 비열로 · Chukoh "비중 2.15"가 실은 **유전율** ·
Parylene HT 인장이 옆의 **Acrylic 열** · 소결자석 표의 Cast/Sintered 열을 뒤바꿔 ·
Microsyst Nanoeng Table 3에 **OCA 행이 아예 없는데** 인접 4개 층 값 중 아무거나 붙일 뻔.

**공통점은 전부 "그럴듯한" 값이었다는 것이다. 범위 검사로는 안 걸린다.**

---

## 4. 파동(wave) 사이클 — 한 바퀴에 무엇을 하고 무엇을 검사하나

한 파동은 대략 **하루~이틀**, 병렬 배치 **6~11갈래**다. 순서는 고정이다.

| # | 단계 | 하는 일 | **무엇을 검사하나** | 도구 |
|---|---|---|---|---|
| 1 | 빈칸 분석 | 격자에서 비어 있는 칸을 뽑는다 | 구조적 부재를 뺀 **채울 수 있는 칸**인가 | `coverage_report.py` |
| 2 | 레버리지 계산 | 무엇을 채우면 몇 칸이 닫히나 | **칸 수가 아니라 칸/재료**로 잰다(§5) | 아래 스크립트 |
| 3 | 타깃 파일 생성 | 배치별 재료 목록 | **무엇이 없어서 이 재료가 뽑혔는지를 함께 적는다** | — |
| 4 | 병렬 배치 투입 | 브리프 + 타깃 파일을 각 배치에 | 배치마다 `scratchpad/<영역>/` 분리 | `COLLECTION_BRIEF.md` |
| 5 | chunk 중간 저장 | 재료 10~12종마다 파일을 쓴다 | 프로세스가 죽어도 남는가 | — |
| 6 | JSON 파싱 검증 | `json.load()` | 파싱 실패 0 | — |
| 7 | 인제스트 드라이런 | `ingest_agent_json.py <dir>` | **6단계 검증**(아래) | 인제스터 |
| 8 | 적용 | `--apply` | 신규 재료·거부 사유 리뷰 | 인제스터 |
| 9 | tier 정규화 | 값의 성격으로 등급 재판정 | 자동 규칙 vs `MANUAL`/`FROZEN` 예외 | `normalize_tiers.py` |
| 10 | 무결성 검사 | 37항목 + 주의 1항목 | **0이 아니면 결함** | `integrity_check.py` |
| 11 | 통계·문서 재생성 | 커버리지·xlsx·docx | 하드코딩 없이 전부 계산값인가 | `build_*` 스크립트 |
| 12 | 이력 기록 → 배포 | 무엇이 틀렸는지까지 | — | `PROPERTY_DATA_HISTORY.md` |

### 4.1 5단계가 왜 규칙이 됐나

8차 파동 첫 투입분 6개가 결과를 **마지막에 한 번만** 쓰도록 돼 있었고,
프로세스 종료로 **산출물이 0**이었다. 여섯 배치가 통째로 날아갔다.
재투입할 때 chunk 저장을 강제했고 이후 중단 없이 회수됐다.
**장시간 배치는 중간 저장을 설계에 넣어야 한다.**

같은 종류로, 7차 파동은 세션 한도로 끊겼는데 **끊긴 배치가 이미 해놓은 일이 기록되지
못했다** — 다음 세션의 피로 배치가 MIL-HDBK-5H를 찾으러 갔더니 앞 세션이 이미 2.28 MB를
받아 스크래치패드에 두었다. **중단된 파동의 스크래치패드를 먼저 뒤져야 한다.**

### 4.2 7단계 — 인제스트가 거부하는 것

**에이전트는 DB에 직접 쓰지 않는다.** SQLite 락 경합을 피하고 등록 전 검토 지점을 만들기
위해 JSON으로 내놓게 하고, 메인이 검증 후 직렬 등록한다.

| # | 검증 | 무엇을 막나 |
|---|---|---|
| ① | `property_key`가 taxonomy에 있는가 | 그럴싸하지만 없는 키를 지어내는 것(`poissons_ratio`, `expansion_coefficient`…) |
| ② | 단위가 정의 단위와 일치하는가 | 곱 순서만 다른 표기(`m²·K/W` vs `K·m²/W`)는 같게 보되 기호가 다르면 거부 |
| ③ | 출처(url/doi/title)가 있는가 | **없으면 항목 전체 건너뜀.** 스키마 설계 때부터 강제라 "출처 없음"이 끝까지 0 |
| ④ | 물리적 범위 안인가 | 40여 키에 범위. **신규 키는 범위 자체가 단위 검산**이다(CS를 MPa 원값으로, DOL을 µm 원값으로 넣으면 걸린다) |
| ⑤ | 비율 물성이 1을 넘지 않는가 | 연신율·흡습률 퍼센트 오입력(100배 오류) |
| ⑥ | 기존 tier1 실측을 덮지 않는가 | **단, 조건이 다르면 별개 측정이므로 통과** |
| + | 조건 충돌 | 조건 서명이 같은데 값이 다르면 **덮어쓰기 대신 거부**하고 사유 보고 |

⑥의 단서가 왜 붙었는지 —
처음엔 "기존 tier1을 덮지 않는다"가 **조건이 다른 별개 측정까지 막았다.**
그리고 반대로, 적재가 (재료·물성·출처·조건)에 멱등이라
**조건이 같고 값만 다르면 나중 것이 앞의 것을 조용히 덮는다.**
3M Viscoelastic 112P05 박리력이 112P02와 시험조건이 같아 이렇게 사라져 있었다 —
그레이드가 `conditions`가 아니라 `note`에만 있었기 때문이다.

### 4.3 10단계 — 무결성 검사 37항목

배포 전 반드시 0으로 통과시킨다. 현재 **37항목 전부 0**, 주의 1항목 41건.

| 검사군 | 항목 예 |
|---|---|
| 참조 무결성 | 출처 없는 값 · 끊어진 출처 참조 · 정의 없는 물성키 · 고아 시편 |
| 어휘·타입 | tier 범위 · method 어휘 · 출처 kind 어휘 · category 어휘 · 조건이 dict 아님 |
| 단위 | 단위가 정의와 다름(곱 순서 정규화 후 비교) |
| 조건 필수 | 초탄성에 model · 광학값에 파장 · 확산계수에 온도 · 노화 유지율 조건 넷 |
| 조건 금지 | **온도가 값인 물성에 온도 조건**(융점·Tg·HDT — 대표값 선택이 무너진다) |
| 모델 세트 | Prony 항번호 · Basquin 쌍 · Coffin-Manson 쌍 · Morrow 쌍 · Darveaux 쌍(홀수) · 항번호 없는 다항 상수 |
| 물리 | **Mooney-Rivlin G0 음수**(Drucker 불안정) · 비금속인데 비열이 금속급 · 파장이 가시광 밖 |
| 가정값 규율 | 가정값인데 tier4·estimated 아님 · 가정값인데 근거 출처 없음 |
| 소비 로직 | **LCSR 곡선 가로축 비단조** — SQL로는 못 잡아 실제 생성기를 돌려 확인 |
| 주의(결함 아님) | 열적 이방성이 입증됐는데 방향 없는 열전도율 — 현재 41건 |

**마지막 항목이 중요하다.** 방향을 지어내서 검사를 통과시키는 것도, 아는 위험을 안 알리는
것도 안 된다. 그래서 결함으로 세지 않고 **경고로 남긴다.**

---

## 5. 우선순위를 어떻게 정하는가 — 이 문서의 핵심

### 5.1 칸 수가 아니라 칸/재료로 잰다

8차 파동 착수 시점에 빈칸이 5,150개였다. 물성별로 세면 **밀도가 893칸**으로 압도적 1위다.
여기까지는 누구나 안다. **그런데 그 893칸의 원인은 재료 134종뿐이었다.**

```
893칸 ÷ 134종 = 재료 하나당 6.7칸
```

밀도는 구조·낙하·열전달·워피지·결로·초탄성·접착 **거의 모든 해석의 필수 물성**이라
한 번 채우면 여러 칸이 동시에 닫힌다. 게다가 근거를 찾기도 가장 쉽다
(결정구조 논문의 계산밀도, 벤더 비중, 같은 계열 값).

**134종에 배치 하나를 붙였더니 전체 셀 채움이 72.2% → 82.3%로 10%p 뛰었다.**
**재료 수가 아니라 칸/재료로 우선순위를 잡은 것이 맞았다.**

### 5.2 그런데 지금은 그 척도가 무력하다

같은 계산을 오늘 다시 돌리면 결과가 완전히 뒤집힌다.

| 물성 / 택일군 | 닫히는 칸 | 재료 수 | **칸/재료** |
|---|---:|---:|---:|
| 소성(항복 OR 인장+연신) | 30 | 30 | 1.00 |
| 피로계수(Basquin·C-M·Darveaux·Morrow·Stromeyer) | 16 | 16 | 1.00 |
| Prony 세트 | 7 | 7 | 1.00 |
| 접착(peel·lap shear·die shear) | 6 | 6 | 1.00 |
| 투습(WVTR·투습계수·확산·흡습) | 4 | 4 | 1.00 |
| **영률** | **3** | **1** | **3.00** |
| 율속(C-S · DIF · σy(ε̇) · JC-C) | 3 | 3 | 1.00 |
| 산소(O₂ 투과·용해도) | 2 | 2 | 1.00 |
| CTE | 1 | 1 | 1.00 |
| **합계** | **72** | | |

**칸/재료가 1.00으로 평평해졌다.** 8차의 밀도(6.7칸 × 134종) 같은 덩어리는 더 이상 없고,
최고인 영률 3.00조차 대상이 액체 전해질 1종뿐이다.
**기본 물성이 닫히면 이 척도는 스스로 수명을 다한다.**

### 5.3 세 번째 척도 — "1칸 부족 재료"를 센다

레버리지가 평평해진 지금 실제로 우선순위를 정하는 것은 **한 칸만 채우면 해석이 열리는 재료의 수**다.

재료 544종 중 빈칸이 있는 것은 **55종**뿐이고, 그 분포가 이렇다.

| 남은 빈칸 | 재료 수 |
|---:|---:|
| 1칸 | **44종** |
| 2칸 | 9종 |
| 3칸 | 1종 |
| 4칸 이상 | 1종 (액체 전해질 7칸) |

해석별로 쪼개면 그림이 더 선명하다.

| 해석 | 남은 재료 | 1칸 부족 | 2칸 | 3칸+ |
|---|---:|---:|---:|---:|
| 낙하·충격 | 32 | **31** | 0 | 1 |
| 피로·수명 | 16 | **16** | 0 | 0 |
| 폴더블 벤딩 | 7 | **7** | 0 | 0 |
| 접착·박리 | 6 | **6** | 0 | 0 |
| 결로(투습) | 4 | **4** | 0 | 0 |
| 산소확산-매트릭스 | 2 | **2** | 0 | 0 |
| 구조·강성 | 1 | **1** | 0 | 0 |
| 열응력·워피지 | 1 | 0 | 1 | 0 |
| 열전달·방열 · 결로(표면) · 초탄성 · 전기·EMI · 산소소광 | 0 | — | — | — |

**해석 13개 중 5개는 이미 남은 재료가 0이다.** 나머지도 대부분 "딱 한 칸"만 모자란다 —
낙하 31종, 피로 16종, 벤딩 7종이 각각 한 칸씩이다.

**이 척도도 수명이 거의 다했다.** 남은 55종은 흩어져 있고, 한 배치가 여러 재료를 함께
닫는 구조가 아니다. 이제부터의 우선순위는 빈칸이 아니라 **가정값 대체**가 정한다 —
(재료 × 물성) 10,890칸 중 **3,219칸(29.6%)이 여전히 tier4로만 대표된다.**

### 5.4 우선순위를 잘못 잡았던 실제 사례

**지표가 틀리면 우선순위도 틀린다.**
낙하가 22%로 꼴찌일 때 "항복강도가 병목"이라고 판단해 **에이전트 하나를 통째로 썼다.**
결과는 "채울 수 없는 항목"이었다 — GF 강화 등급은 항복점을 애초에 재지 않는다.
지표를 고치니 낙하는 74%였고 **진짜 병목은 율속(305종 전무)**이었다(58장).

---

## 6. 지표 네 가지와 각각이 감추는 것

### 6.1 정의

| 지표 | 정의 | 재는 것 | **감추는 것** |
|---|---|---|---|
| **셀 채움** | 채워진 칸 / 전체 칸 | 수집 진척도 | **값의 품질.** tier4 가정값도 채운 것으로 센다 |
| **실측기반** | tier4를 뺀 칸 / 전체 칸 | 근거의 두께 | 재료마다 다른 칸이 비었다는 사실 |
| **재료 준비** | 그 해석을 돌릴 수 있는 재료 / 대상 재료 | **해석 가능성** | 그 카드가 가정값으로 서 있는지 |
| **적용 대비 준비** | 준비된 재료 / **원리적으로 적용 가능한** 재료 | 도달 가능한 천장 대비 | 분모를 줄여 올린 수치라는 점 |

### 6.2 현재 값 (2026-08-10 · 13차 파동 마감)

| 해석 | 대상 | 셀 채움 | 실측기반 | 재료 준비 | 적용 대비 | **실측 기준 준비** |
|---|---:|---:|---:|---:|---:|---:|
| 구조·강성 | 544 | 99.9% | 58.4% | 99.8% | 99.8% | **33.5%** |
| 낙하·충격 | 544 | 95.0% | 49.0% | 84.7% | 93.5% | 13.8% |
| 열전달·방열 | 544 | 100.0% | 67.6% | 100.0% | 100.0% | **39.0%** |
| 열응력·워피지 | 544 | 99.9% | 59.5% | 99.8% | 99.8% | **31.8%** |
| 폴더블 벤딩 | 185 | 99.1% | 60.1% | 96.2% | 96.2% | 23.2% |
| 결로(표면) | 544 | 97.5% | 68.3% | 89.9% | 100.0% | 35.3% |
| 결로(투습) | 417 | 96.8% | 61.9% | 87.1% | 98.9% | 27.6% |
| 초탄성(실링·완충) | 46 | 100.0% | 82.6% | 100.0% | 100.0% | 67.4% |
| 접착·박리 | 185 | 88.1% | 68.9% | 76.2% | 95.9% | 41.6% |
| 전기·EMI | 544 | 100.0% | 75.2% | 100.0% | 100.0% | 75.2% |
| 피로·수명 | 198 | 87.2% | 73.2% | **61.6%** | 88.4% | 34.8% |
| 산소소광-발광체 | 55 | 100.0% | 100.0% | 100.0% | 100.0% | 100.0% |
| 산소확산-매트릭스 | 79 | 97.5% | 82.3% | 97.5% | 97.5% | 82.3% |
| **전체** | | **97.4%** | **61.8%** | | | |

### 6.3 지표 하나만 보면 속는 자리 — 실제 수치로

**(가) 셀 채움만 보면 "거의 다 됐다"로 읽힌다.**
구조·강성은 셀 99.9%다. 그런데 **실측기반은 58.4%**이고,
**실측만으로 카드가 나오는 재료는 33.5%(182종)**뿐이다.
포아송비 713건 중 **408건(57.2%)이 tier4**이기 때문이다.
전체로는 **5,161칸(35.6%p)이 가정값이 만든 착시**다.

**(나) 재료 준비만 보면 "실측으로 서 있는 줄" 안다.**
열전달·방열은 준비율 99.8%(543종)인데 실측 기준으로는 **37.1%(202종)**다.
열전도율 760건 중 306건, 비열 610건 중 214건이 tier4다.
**덱은 543종이 돌지만, 그중 341종은 가정값을 딛고 서 있다.**

**(다) 셀 채움과 재료 준비가 크게 갈리는 자리가 진짜 병목이다.**
피로·수명은 셀 75.4%인데 준비 26.3%다. 낙하는 82.2% 대 34.9%다.
**재료마다 비어 있는 칸이 다르면 이렇게 벌어진다.**
반대로 전기·EMI는 93.9% 대 93.9%로 완전히 같다 — 택일군 하나짜리 해석이라
채운 칸이 곧 준비된 재료다.

**(라) "적용 대비 준비"는 분모를 줄여 올린 수치다.**
세라믹에 변형률-수명 피로계수를 요구하면 그 재료는 영원히 준비 안 됨으로 남는다.
알루미나로 strain-life 피로해석을 돌리는 일은 없으므로 분모에서 뺀다.
피로가 26.3% → 33.8%로 오르는데, **이 상승은 수집 성과가 아니라 정의다.**
그래서 두 열을 반드시 나란히 둔다.

---

## 7. 검증 장치 다섯 — 그리고 각각이 실제로 잡은 것

### ① 무결성 검사 37항목 — 구조를 본다

| 검사 | 실제로 잡은 것 |
|---|---|
| Mooney-Rivlin G0 음수 | Dragon Skin 10 MEDIUM(−86.0 kPa)·Ecoflex 00-50(−315.9 kPa). **초기 전단탄성률이 음수면 Drucker 안정조건 위반이라 해석이 발산한다.** 4건 삭제 후 상시 항목으로 승격(56장) |
| 비금속인데 비열이 금속급 | EMC 236 J/(kg·K). 넣자마자 걸린 3건은 **전부 오탐이었고 오탐의 원인이 진짜 결함**이었다 — SmCo·Alnico·VACOFLUX의 `category`가 통째로 뒤섞여 있었다(31장) |
| 온도가 값인데 온도 조건 | 융점·Tg·HDT 48건. 텅스텐 융점 3410 °C가 "상온에서 3387 °C 떨어진 값"으로 계산돼 대표값에서 맨 뒤로 밀렸다(19장) |
| 조건이 dict 아님 | `mapped_column(JSON)`이 파이썬 `None`을 SQL NULL이 아니라 **JSON 문자열 `'null'`**로 직렬화하고 있었다. `none_as_null=True` 한 줄로 두 경로가 동시에 고쳐졌다(27장) |
| LCSR 가로축 비단조 | Kapton HN이 298 K에서 ID·TD 두 방향을 각각 3율속으로 재는데 온도만 보고 계열을 묶어 **가로축이 중복되고 배율이 역행하는 `*DEFINE_CURVE`가 나가고 있었다**(59장) |
| Basquin 쌍 불일치 | VACOFLUX 50이 σf′를 **σf′/E(무차원)로만** 인쇄해서 지수만 넣은 것을 결함으로 잡았다. `conditions.pair_incomplete`에 사유를 적은 행은 빼도록 고쳤다 — **사유 없는 반쪽만 잡는다**(69장) |

### ② 반례 검증 — 선언은 주장이 아니라 반례로 무너진다

"이 물성은 이 재료군에서 애초에 발표되지 않는다"는 선언은 **두 조건을 다 만족할 때만** 유효하다.

1. 문서에 기록된 사유가 있다(`UNFILLABLE`에 적혀 있다) — "아직 못 찾았다"와 구분한다.
2. **그 군의 어느 재료도 그 물성을 실측으로 갖고 있지 않다 — 반례가 0이다.**

값이 하나라도 들어오면 그 쌍은 **자동으로 부재에서 빠진다.**

- 처음 `UNFILLABLE`을 그대로 믿었더니 **102칸이 "부재인데 값이 있음"으로 걸렸다.**
  벤조페논은 상용 화학물질이라 CRC에 밀도가 있고, `laminate`·`coverlay` 매처가 비열을 가진
  재료까지 잡았다. **선언 16개 중 13개가 반례로 무너졌다**(70장).
- **반례는 실측(tier≤3)만 인정한다.** 8차에서 아크릴 폼 테이프 11종에 항복강도 tier4
  가정값을 넣자 "점착테이프에는 항복점이 물리적으로 없다"는 선언이 **자동 은퇴했다.**
  **우리가 만든 가정이 우리의 부재 선언을 지우면 안 된다**(71장).
- **페이월은 부재가 아니다.** 접근 문제는 언제든 열릴 수 있으므로 분모에 남긴다.

### ③ 자기 검산 — 얻은 값으로 그 논문의 다른 값을 예측한다

| 검산 | 결과 |
|---|---|
| Ti-6Al-4V Johnson-Cook 세트 | 그 세트로 예측한 응력 **2725 MPa** 대 같은 논문 실측 **1680 MPa**. 폐기 |
| PC 비열 표 | 그 논문의 PC 밀도가 **1.44 g/cm³**다. 실제 PC는 1.20. 표 폐기 |
| FR-4 열물성 표 | 같은 표의 **Sn63Pb37 밀도가 1,890 kg/m³**(실제 ≈8,400). **알려진 재료로 표 전체를 검산**해 통째로 버렸다(68장) |
| EMC 비열 (혼합물 상하한) | EMC는 에폭시(cp≈1,200)와 실리카(≈740)의 혼합물이라 **1,200 초과나 740 미만은 불가능**. 논문 표 두 건(1,672 · 236)을 걸렀다. **둘 다 같은 표의 다른 행은 완벽했다** — 행 단위 판단(71장) |
| Prony Σg | Microsyst. Nanoeng. 본문 마스터커브가 **g1+g2+g3 = 2.11**로 상대계수가 성립하지 않았다. 반대로 FR-4는 Σα = 0.9999·1.0022가 나와 **그 열이 상대계수임을 확정**했다. 같은 검산이 버리는 데도 확정하는 데도 쓰인다(65장) |
| Meggiolaro-Castro 추정식 | FD&E **실측 피팅 54건으로 자기 검산**. σf′/Su 실측 중앙값 대 추정 — 탄소강 1.41 대 1.5 ✓, Ni계 1.56 대 1.4 ✓, **스테인리스 2.15 대 1.5 ✗**. 편향 방향을 notes에 적고 SUS301은 실측 계열값으로 덮었다(72장) |
| Gent(1958) 상관식 | 적용범위(20–80 Shore A) 안에서만 쓰되, **충전 실리콘에서 2배 과대평가**함을 확인해 notes에 남겼다(72장) |
| 물리 한계로 단위 확정 | PORON 시트가 `g/ft²/24hrs (g/m²/24hrs)` 두 단위를 병기하고 값은 하나. g/ft² 해석(36,600)이 37 °C 3 mm 공기층의 **순수확산 한계 ~32,800**을 넘어 불가능하므로 g/m²로 확정(68장) |
| 단위 머리글 검산 | `PMC12300880` Table 4의 `ε_b (mm/mm) 730`은 73,000 %라 NR에 불가능. 같은 행의 파단에너지(32.2 J/cm³ ÷ 7.30 = 4.4 MPa)로 검산해 **730 %**로 확정 |

### ④ 원문 문자열 대조 — 저장한 숫자가 인용 출처에 실제로 있는가

수집을 마친 뒤 **각 행의 값이 인용 출처 원문에 문자열로 존재하는지** 검사하는 스크립트를 돌린다.
5차 파동에서 한 배치가 자발적으로 만들었고, 그 뒤 브리프의 작업 습관이 됐다.

| 잡은 것 | 실체 |
|---|---|
| Al2024-T3 표면에너지 35.3 mJ/m² (66장) | 값은 맞는데 **엉뚱한 논문에 붙어 있었다** |
| Lexan FR25A 탄성계수 (68장) | SABIC 매뉴얼 출처에 붙었으나 실제는 arXiv 논문 |
| FR-4 밀도 (68장) | 값은 `g-10.pdf`에 인쇄됐는데 블록 URL이 `fr-4.pdf`. **인제스트는 블록당 출처 하나만 저장하므로 그대로면 엉뚱한 문서에 붙는다** |
| 아크릴 PSA 열전도율 0.176 (71장) | **원문 전문에 `0.176`이 0회 등장**. notes가 주장한 "0 wt% 대조군 실측"이 거짓이었다 |
| Copper Pillar Bump 항복강도 3건 (69장) | tier1 실측으로 있었으나 원문이 *"substitute … to obtain the **theoretical** yield strength"*라고 밝힌 계산값 |

**여기서 나온 교훈이 이 문서에서 가장 중요한 것 중 하나다.**
notes가 주장하는 근거를 원문에서 확인하지 않으면 **틀린 값이 아니라 틀린 근거가 번진다.**
**값은 검산으로 걸리지만 근거는 안 걸린다.**

### ⑤ 규칙 6 — 기존 값과의 자릿수 대조

§3의 규칙 6이 그대로 검증 장치다. **넣기 전에 DB에서 그 재료의 기존 값을 먼저 확인한다.**
값 하나만 보면 완벽한데 그 재료의 카드 안에서 앞뒤가 안 맞는 경우를 잡는다.

### 7.1 검사가 오탐을 냈을 때의 규율

세 번 있었고, 대응이 매번 같았다 — **검사를 느슨하게 만들기 전에 오탐의 원인부터 본다.**

| 오탐 | 원인 | 고친 방식 |
|---|---|---|
| `가정값인데 tier4 아님`이 SmCo 3건을 잡음 (68장) | 조건에서 **부분문자열** `assumption`을 찾는데 `direction: "bulk effective (isotropic assumption)"` 같은 **방법 서술**에 걸렸다. 값은 초음파 실측 | 구조화된 표지 `"assumption": true`만 보도록 좁혔다. **검사는 산문이 아니라 구조를 봐야 한다** |
| `Prony 항번호 없음`이 36건 오탐 (72장) | **장기계수 E∞는 Prony 항이 아니라 평형값**이다. 급수는 E(t)=E∞+ΣEᵢexp(−t/τᵢ)이고 E∞는 급수 밖이라 항번호가 없는 게 맞다 | 검사에서 제외 |
| `recaptcha` 문자열로 실패 판정 (69장) | **정상 페이지의 폼 스크립트에도 그 문자열이 있다.** 한 배치가 caplinq 물성표 6건을 그렇게 잘못 버렸다 | 판정을 **"찾는 제품명이 본문에 실제로 있는가"**로 바꿨다 |

그리고 반대 방향 — **가드가 좁아서 정상값을 버린 사례가 셋**이다.
굴절률 가드 0.8~5.0이 c-Si(5.613 @400 nm)와 TiN(0.7946 @633 nm)을 버렸고,
유전율 상한 1e5가 MnZn 페라이트의 정상값 14만(입계 효과)을 버렸고,
Prony 완화시간 상한이 마스터커브 피팅값 1e14 s를 버렸다.
**가드는 자릿수 사고만 잡게 두고 물리 범위를 좁게 잡지 말 것.**

---

## 8. 함정 지도가 축적되는 방식 — 왜 실패를 남기는가

### 8.1 실패는 세 곳에 나눠 적는다

| 문서 | 무엇을 담나 | 목적 |
|---|---|---|
| `COLLECTION_BRIEF.md` **6절** | **판정 함정** — 어떤 표·사이트·표기를 조심할 것인가 | 다음 배치가 같은 값을 다시 집지 않게 |
| `BLOCKED_SOURCES.md` | **접근 실패** — 유료(A)/로그인(B)/미공표(C)/삭제(D)/그림뿐(E) | 같은 곳을 다시 뒤지지 않게 |
| `PROPERTY_DATA_HISTORY.md` | **경위** — 무엇을 시도했고 왜 틀렸나 | 판단의 근거를 재구성할 수 있게 |

브리프 정본의 첫 문장이 이렇게 적혀 있다 —
**"이 문서의 값어치는 규칙이 아니라 6절의 함정 지도에 있다. 각 항목은 전부 실제 사고 기록이다."**

### 8.2 실제로 시간을 아낀 증거

**(가) 검산 방법이 전파돼 다음 파동에서 즉시 작동했다.**
6차 파동이 FR-4 열물성 표를 **같은 표의 알려진 재료(Sn63Pb37 밀도 1,890)로 검산해** 버렸다.
이 방법을 브리프 6절에 "표에 있는 알려진 재료로 검산하라"로 박았고,
**8차 파동은 같은 방식(혼합물 물리 상하한)으로 EMC 논문 표 두 건을 즉시 걸렀다.**
두 건 다 같은 표의 다른 행(Si 712, PCB 920, Al 900)은 완벽했으므로,
검산 규칙이 없었으면 표 전체를 신뢰했을 것이다.

**(나) 복사 계보 규칙이 여러 건을 한 번에 버리게 했다.**
PV 적층 물성표가 세 문헌에 바이트 단위로 복사돼 있음을 확인한 뒤로,
**같은 숫자 조합이 여러 논문에 나오면 한 건만 검산하고 나머지를 같이 버린다.**
독립 확인처럼 보이는 것을 세 번 검증하는 낭비가 사라졌다.

**(다) 아카이브 회수분이 다음 파동의 첫 조회처가 됐다.**
9차 피로 배치가 `fde.uwaterloo.ca`를 무지연으로 610쪽 받고 **IP 차단**을 맞았다.
Wayback으로 26개 세트를 회수해 `FDE_FATIGUE_ARCHIVE.md`에 표로 남겼고,
문서 첫 줄이 **"다음 파동은 이 표를 먼저 보고 필요한 것만 Wayback에서 다시 받아라"**다.
같은 문서가 **회수하지 못한 것**(AA7050-T7351, PPS-glass)도 명시해
다음 파동이 그것을 찾느라 시간을 쓰지 않게 했다.

**(라) 광맥 하나를 지도에 올린 것이 한 배치를 통째로 해결했다.**
브리프 2절에 `accudynetest.com`의 `polytable_03.html`이
**"38종 폴리머의 임계표면장력과 물 접촉각을 한 행에 나란히 인쇄해 두 키를 한 출처로 동시에 닫는다"**고
적혀 있다. 9차 젖음성 배치가 **182종 중 179종을 닫았고 그중 133종(74%)이 여기서 나왔다.**

**(마) 브리프의 "성공처럼 보이는 실패" 목록이 잘못된 결론을 막았다.**
`hdl.handle.net`이 862바이트 스텁을 준다는 항목이 없었다면
7차 파동이 **그 스텁을 근거로 "그 논문에 값이 없다"고 결론냈을 것이다.**

### 8.3 0건 보고를 실패로 읽지 않는다

**"더 찾아도 안 나온다"를 확정하는 것도 성과다.**
3차 파동의 0건 보고 세 건이 전부 값 있는 결과로 바뀌었다 —
점탄성 0건은 사유가 등급 불일치임을 밝혀 제약을 풀자 4종 115항이 나왔고,
무기 비열 0건은 CCL 벤더 6곳이 일관되게 비열을 인쇄하지 않는다는 **구조를 확정**했으며,
테이프 밀도 0건은 요청 밖에서 관찰한 12건으로 **기존 DB 17건을 교차검증**했다(불일치 0).

**어디를 그만 파야 하는지가 어디를 더 파야 하는지만큼 중요하고,
이번엔 그 판단이 다음 배치의 방향을 세 번 바꿨다.**

---

## 9. 신뢰도 고도화 경로 — 가정값을 실측으로 갈아 끼우는 구조

### 9.1 네 단 사다리

```
   tier4  근거 있는 가정 ─┐
        (계열 통계 중앙값, 공표된 추정식)   assumption:true + method:estimated
                          ↓  실측이 들어오면 대표값 선택에서 자동으로 밀린다
   tier3  계열 대표값 ────┤
        (같은 계열 다른 제품의 실측, 2차 인용, 등급 불일치 대체)
                          ↓
   tier2  핸드북·표준 ────┤
        (MMPDS · ASM · NIST · CDA · ISO/ASTM)
                          ↓
   tier1  그 제품 실측 ───┘
        (벤더 TDS · 그 재료를 시험한 논문)
```

**갈아 끼우는 데 사람 손이 필요 없다.** 대표값 선택이 tier를 우선 순위로 쓰므로
tier1 행이 하나 들어오는 순간 조회·비교·DYNA 카드가 전부 그쪽을 쓴다.
tier4 행은 삭제하지 않고 남긴다 — **불확실성 범위와 이력을 보존하기 위해서다.**

### 9.2 지금 tier4가 몇 건이고 어디에 몰려 있나

tier4는 **4,490건(21.1%)**이다. 그중 `assumption:true` 표지를 가진 것이 **4,398건**이고,
나머지 92건은 전부 `computed`로 **가정이 아니라 계산·유도값**이다.

**물성별로 어디에 몰렸나** (재료 50종 이상 보유 물성 중 tier4 비율 상위)

| 물성 | tier4 / 전체 | tier4 비율 | 성격 |
|---|---:|---:|---|
| 동적증가계수(DIF) | 450 / 500 | 90.0% | 율속 배치의 **무차원 계열 대입**. 절대강도 대신 DIF로 옮겨 규칙 6을 피했다 |
| 전반사율(emissivity) | 202 / 218 | 92.7% | 표면 상태가 지배하는데 벤더가 안 낸다 |
| 아웃가싱 CVCM/TML | 38 / 51 | 74.5% | **값의 정체가 ASTM E595 합격 상한선**이다(13차에 `bound=upper`로 명시) |
| 흡습 포화율 | 96 / 141 | 68.1% | ISO 62 계열 |
| **포아송비** | **408 / 713** | **57.2%** | **가장 큰 덩어리.** 클래스 가정 8개 숫자에서 복제 |
| Johnson-Cook C | 31 / 62 | 50.0% | 율속항 |
| 항복강도 | 105 / 233 | 45.1% | 8차 방침으로 닫은 분 |
| **열전도율** | **307 / 804** | **38.2%** | 계열 대입 |
| **비열** | **203 / 608** | **33.4%** | CCL·테이프·연자성재는 벤더가 아예 안 낸다 |

**재료 카테고리별** — 폴리머 2,254 · 복합재 766 · 금속 403 · 세라믹 295 · 폼 134 · 고무 66.
**tier4의 57%가 폴리머에 있다.** 6차 파동에서 확인한 대로
**구멍이 거의 전부 폴리머·복합·고무·폼에 몰려 있고 금속·세라믹은 사실상 다 찼다.**

**재료 단위 최다** — Glass-Filled Nylon (PA6-GF30) 55건, Coverlay (PI + adhesive) 54건,
Kapton PI Adhesive Tape 40건, Arisawa C33 Low-Dk Coverlay 40건.

### 9.3 포아송비 — 가정값의 실제 불확도가 측정된 사례

포아송비 tier4 317건이 **여덟 개 숫자에서 복제**돼 있었고 `0.490` 하나가 106건이었다(68장).

그런데 **Isola가 모든 라미네이트 TDS에 ν를 length/cross로 ASTM D3039 실측으로 인쇄한다.**
실측 스펙트럼이 **FR408HR 0.137 ~ I-Tera MT40 0.234**다.
**"직물유리 적층판 ν=0.183"이라는 클래스 가정 하나가 20건에 복제돼 있는데 실제 폭이 0.13~0.23이다.**

같은 파동에서 두 가지가 더 드러났다.

- **3M VHB 아크릴 폼 테이프의 ν는 0.49가 아니라 0.35다.** 3M 공식 TDS의 2018·2019 개정판에
  `Poisson's Ratio 0.35` 행이 있는데 **3M이 2022년 이후 개정판에서 이 행을 삭제**했다.
  폼 테이프 8종의 0.30/0.49 가정이 전부 틀렸다.
- **PA6-GF30이 등방이 아니었다.** 유동방향 0.43/0.39, 직교방향 0.22/0.27로
  **2배 방향 편차**를 단일 가정값 0.35가 덮고 있었다.

**교훈은 tier4를 tier1로 갈아 끼우는 일이 "정밀도 개선"이 아니라
"틀린 값의 교체"인 경우가 있다는 것이다.**

### 9.4 고도화의 다음 표적

1. **포아송비 413건** — Isola처럼 ν를 인쇄하는 벤더를 더 찾는 것이 가장 큰 효과다.
   다만 **OLED 저분자 51종은 실측이 문헌에 존재하지 않는다** — 대표 나노압입 연구조차
   ν를 몰라 `E/(1−ν²)` reduced modulus로만 발표한다.
2. **열전도율 306건 · 비열 214건** — CCL·테이프·연자성재·커버글래스는 벤더 전수 확인으로
   **구조적 부재가 확정**됐다. 남은 경로는 실측 위탁뿐이다.
3. **DIF 236건** — 무차원 계열 대입이라 자릿수 모순은 없지만 그 재료의 값은 아니다.
   OA 밖(핸드북·벤더 시험보고서)을 봐야 한다.

---

## 10. 현재 상태와 남은 일

### 10.1 규모 (2026-08-10, DB 직접 조회)

| 항목 | 값 |
|---|---:|
| 재료 | **544종** (polymer 300 · metal 80 · composite 71 · ceramic 47 · rubber 24 · foam 22) |
| 물성값 | **21,279건** |
| 출처 | **2,134건** (DOI 보유 733 · URL 보유 1,977) |
| 물성 정의 | **161종 — 전부에 값이 있다(개통률 100%)** |
| 시편 | 73건 |
| 무결성 | **37항목 전부 0** · 주의 1항목(방향 없는 열전도율) 41건 |

**출처 종류** — datasheet 879 · journal 714 · database 313 · other 105 · book 72 ·
standard 26 · web 24 · computed 1.

**도메인별 정의/값** — mechanical 58종/9,987건 · optical 18/1,361 · thermal 16/3,933 ·
chemical 15/738 · physical 13/1,878 · electrical 11/1,716 · interface 9/798 ·
structure 7/324 · rheological 5/195 · magnetic 5/201 · acoustic 4/148.

### 10.2 격자 현황

| | 칸 |
|---|---:|
| 전체 격자 | **14,478** |
| 채워진 칸 | 14,102 (97.4%) |
| 그중 실측기반(tier≤3) | 8,941 (61.8%) |
| **가정값이 채운 칸** | **5,161 (35.6%p)** |
| 빈칸 | 376 |
| 그중 구조적 부재라 못 채우는 칸 | 304 |
| **실제로 채울 수 있는 남은 칸** | **72** |
| (참고) 실측 기준으로 남은 일 | 5,537칸 |

### 10.3 남은 72칸의 성격

| 덩어리 | 칸 | 왜 어려운가 |
|---|---:|---|
| 소성(낙하) | 30 | n-알킬 아크릴레이트 6종은 앵커 논문에 인장강도는 있는데 **변형률이 전문에 0회**다. 고무계 PSA는 계열 후보가 택키파이어 40~60 % 배합을 한 자릿수 과대평가한다 |
| 피로계수 | 16 | METGLAS·나노결정 FeSiB·분말코어·ACF/DAF/Ag에폭시·E-glass·커버레이 — **근거가 검색 소진뿐이라 부재로 선언하지 않고 남긴 칸**이다 |
| Prony | 7 | 상용 LCP·PEI·PEN 필름의 점탄성은 Prony로 발표되지 않고, 택일군의 전단탄성률도 벤더가 인쇄하지 않는다 |
| 접착 | 6 | 박막/후막 밀착은 스크래치 임계하중·계면파괴에너지로 재는데 브리프가 그 키 이동을 금한다 |
| 투습 | 4 | 액체 전해질·Ir 인광체는 시편 자체가 성립하지 않는 경계 사례다 |
| 율속(낙하) | 3 | 하이드로겔·PET 부직 펠트의 인쇄된 DIF가 없다. 벌크 PET DIF를 12.7 mm 펠트에 옮기는 것은 계열 전파 금지에 걸린다 |
| 영률·CTE | 4 | 액체 전해질은 전단저항이 없어 E가 정의되지 않는다 |
| 산소 | 2 | 치밀 금속 배리어와 RF 적층판은 어느 쪽도 기체투과도를 발표하지 않는다 |

**남은 72칸은 55종에 흩어져 있다.** 파동 초기처럼 한 물성을 뚫어 수백 칸이 닫히는 자리는
더 이상 없다 — 이제부터는 재료 하나에 문서 하나씩이다.

### 10.4 남은 일의 성격 요약

- **10개 해석은 재료마다 딱 한 칸씩 모자란다**(§5.3). 물성 하나를 뚫으면 해석 하나가
  통째로 100%가 되는 자리가 열 군데다.
- **낙하·충격만 2층 병목**이다(129종이 소성+율속 동시 결측).
- **막힌 경로가 바뀌지 않는 한 72칸 전부는 안 닫힌다.** 여러 파동에 걸쳐 확인된 결론은
  율속과 피로가 **OA 밖(핸드북·벤더 시험보고서·유료 표준)**에 있다는 것이다.
- **동시에, 채워진 14,102칸 중 5,161칸이 가정값이다.** 남은 칸을 닫는 것과
  이미 닫은 칸의 근거를 두껍게 하는 것은 **별개의 일이고 후자가 압도적으로 크다.**
  (재료 × 물성) 10,890칸 중 **3,219칸(29.6%)이 가정값으로만 대표된다.**

---

## 11. 이 방법론이 틀렸던 순간들

**이 절이 이 문서에서 가장 중요하다.**
아래는 전부 우리가 세운 가설·지표·지시가 반례로 무너진 기록이다.
**자기 교정이 이 시스템의 핵심이고, 그 교정은 예외 없이 "숫자가 이상하다"에서 시작됐다.**

### 11.1 구조적 부재 22% 추정이 0.7%로 밝혀졌다 (70장)

"셀 채움 64.5%"의 천장이 100%가 아닐 것 같아 구조적 부재를 분리하려 했다.
착수 전 추정은 **"544종 중 124종(22%)이 못 채우는 재료라 8%p가 천장 아래"**였다.

**틀렸다. 재료 단위로 통째 제외해서 나온 숫자였다** — 이미 채워진 칸까지 버린 계산이다.
부재를 (재료군 × 물성) 쌍으로 선언하고 반례 검증을 붙이니 **선언 16개 중 13개가 무너졌고**
실제 구조적 부재는 **106칸(0.7%)**뿐이었다.

**천장은 100%에 가깝고, 남은 일은 내가 말한 것보다 훨씬 많다.**
앞선 파동들의 "확정된 부재" 보고는 **특정 제품을 못 찾았다**는 뜻이지
그 물성이 그 재료군 전체에서 발표되지 않는다는 뜻이 아니었다. **그걸 일반화한 것이 오류였다.**

### 11.2 지표가 자기 가정값에 속고 있었다 — 26배 큰 왜곡 (70·71장)

천장(분모)을 재려던 시도가 바닥(분자)을 드러냈다.

| | 셀 채움 | 재료 준비(구조·강성) |
|---|---:|---:|
| 당시 지표(가정 포함) | 64.5% | 61.2% |
| **tier4 제외(실측 기반)** | **46.3%** | **27.8%** |
| 차이 | **18.1%p · 2,628칸** | 33.4%p |

**구조적 부재(0.7%)를 찾으러 갔는데 그보다 26배 큰 왜곡을 찾았다.**
지표를 의심할 때는 분모만이 아니라 분자도 봐야 한다.

그리고 8차에서 그 반례 검증 장치 자체가 **자기 가정값에 속았다** —
항복강도 배치가 아크릴 폼 테이프 11종에 tier4 가정값을 넣자
"점착테이프에는 항복점이 물리적으로 없다"는 선언이 자동 은퇴했다.
반례 판정이 tier를 구분하지 않고 있었고, 그 경고는 **157종을 오탐**하고 있었다.

**방침을 바꾸면 그 방침이 기존 장치를 어떻게 흔드는지도 봐야 한다.**
"tier4로 칸을 채운다"는 결정 하나가 **부재 판정·경고·커버리지 세 곳을 동시에 흔들었다.**

### 11.3 "G가 최단 경로"라는 지시가 틀렸다 (72장)

9차 Prony 배치에 **"G부터 훑는 것이 최단 경로"**라고 지시했다.
8차에서 VHB TDS가 G를 인쇄하는 것을 봤기 때문이다.

**틀렸다 — 145종 중 자기 제품의 G를 인쇄한 벤더는 0곳**이고 G 단독으로 닫힌 것은 3종뿐이다.
반대로 **Prony 세트는 포장·디스플레이 FEA 논문 표에 계속 실린다.**
배치가 방향을 틀어 119종을 1,490행으로 닫았다.

**한 사례(VHB)에서 일반 규칙을 만든 것이 오류였다.**

### 11.4 브리프 자신의 URL이 404였다 (72장)

브리프 2절이 accudynetest 색인을 `polytable_01` ~ `04`라고 적어 왔는데 **04는 404다.**
여러 파동이 그 문장을 그대로 복사해 쓰고 있었다.
9차 젖음성 배치가 확인해 정정했고, 동시에 `polytable_03`이
**38종의 임계표면장력과 물 접촉각을 한 행에 나란히 인쇄**한다는 것을 찾아
두 키를 한 출처로 닫는 경로를 열었다.

**지시서도 1차 자료로 취급해 검증해야 한다.**

### 11.5 오탈자 하나가 근거 1,590건을 지우고 있었다 (67장)

`ingest_agent_json.py`가 `pr.get("note")`(단수)만 읽는데
브리프의 출력 예시는 `"notes"`(복수)였다.

**근거 문구가 조용히 전부 사라졌다** — 최근 인제스트분의 **37%**, 전체로는 1,590건.

이 유실이 특히 나쁜 이유가 있다. **값은 남고 "왜 그 값인가"만 없어진다.**
무결성 검사는 값·단위·조건만 보므로 이 결함에 걸리지 않는다.
그런데 사라진 것이 이 카탈로그에서 가장 값진 부분이다 —
열 머리글을 어떻게 확인했는지, 단위를 어떻게 환산했는지, 등급이 무엇이 다른지.

3~5차 파동 산출물 38개 파일을 전부 재적재해 1,590 → 950건으로 줄였다.
(현재 952건. 남은 것은 산출 JSON이 없는 초기 파동분이라 복구 경로가 없다.)

**같은 종류의 사고가 세 번째다** — `tier` vs `quality_tier`(377건 강등),
`volume_resistivity` vs `resistivity_volume`(**존재하지 않는 키를 세고 있었다**), 그리고 이것.
**로더와 브리프의 키 이름이 어긋나면 조용히 유실된다.**

### 11.6 척도가 여섯 번 거짓말을 했다

| # | 척도 오류 | 실제 |
|---|---|---|
| 1 | 전기·EMI가 `volume_resistivity`·`loss_tangent`를 셈 | **taxonomy에 없는 키다.** 유전율 하나만 세어 29.0%였고 실제는 46.5% |
| 2 | 산소 소광 0.0% | 92종을 한 스코프에 넣고 고립 분자에 투과계수를, 벌크 필름에 여기수명을 요구. **71종이 원리적으로 못 채우는 칸**을 세고 있었다 |
| 3 | 흡습에 24h만 요구 | 유럽식 ISO 62 시트는 24h를 아예 발표하지 않는다. 18종을 벤더 문서까지 확인했고 24h가 있는 건 2종뿐 |
| 4 | 값이 잘못된 키에 들어가 안 보임 | Rogers·Taconic 흡습값이 `moisture_absorption_equilibrium`에 있었는데 **IPC-TM-650과 ASTM D570은 침지시험**이다 |
| 5 | 낙하 준비율 22% | 소성 입력을 "항복강도"로만 세어 **146종이 부당하게 미달**. 택일로 고치니 74%, 진짜 병목은 율속 |
| 6 | 셀 채움이 tier를 구분 안 함 | §11.2 |

**같은 종류의 문제가 한 세션에서 네 번 나온 적도 있다.**
**물성을 가졌다는 것과 해석이 그걸 찾을 수 있다는 것은 다르다.**

### 11.7 지시서의 타당성 대역이 판정 기준으로 오용될 뻔했다 (41·43장)

브리핑에 "EMC의 Csat는 수~수십 mol/m³"라고 적었는데 **한 자릿수 낮았다.**
실제 인쇄값은 EMC 200~310, PI 650~1420 mol/m³이고 wt% 환산으로 검산하면 전부 타당하다.

**에이전트가 내 기대 대역을 믿고 버렸다면 유효한 데이터를 전부 잃었을 것이다.**

그래서 **"내가 주는 대역은 참고이지 판정 기준이 아니다"**를 브리핑에 넣었고,
이 한 줄이 이후 에이전트의 자기 정정을 **세 번** 끌어냈다.

같은 종류로 — **검산 밴드가 재료군 중립이 아니다.**
마그네슘 LCF는 쌍정 비대칭 때문에 c가 −1 근처, εf′가 수 단위로 나오는 게 정상인데
강·알루미늄 기준 밴드를 그대로 적용하면 Mg는 계속 폐기 대상이 된다.

### 11.8 브리프를 목록 확인 없이 썼다 (65장)

**"1순위는 솔더"라고 썼는데 담당 목록 78종에 솔더가 한 종도 없었다.**
카탈로그의 유일한 솔더 SAC305는 이미 피로 키 7종을 다 갖고 있어
"피로계수 전무" 목록에 들어갈 수 없었다.

같은 유형으로, 대상 목록 생성 조건이 `열전도율 없음 OR 비열 없음`이라
에이전트가 "이미 있는데 왜 목록에?"라고 잘못된 결함 신고를 했다(54장).
**대상 목록에는 무엇이 없어서 뽑혔는지를 함께 적어야 한다.**

### 11.9 되돌릴 수 없는 손실 (72장)

피로 배치가 `fde.uwaterloo.ca`를 **무지연으로 610쪽 받은 뒤 IP 차단**을 맞았다.
30분 뒤에도 루트 인덱스까지 죽어 있었고, 그 때문에
**AA7050-T7351 실측 피팅과 유일한 복합재 후보(PPS-glass)를 놓쳤다.
둘 다 Wayback에도 없다.**

**이것만은 사후 교정이 불가능했다.** "디렉터리 순회에 지연을 넣어라"를 6절에 박았지만
잃은 것은 돌아오지 않는다.

### 11.10 이번 문서를 쓰면서 새로 발견한 것 둘

**(가) `coverage_report.py`의 "유효 채움"이 부재 칸을 이중으로 셌다. — 고쳤다.**
구조적 부재 칸 중 상당수에 이미 tier4 가정값이 들어 있다(정상이다 — 8차 방침대로다).
그런데 지표는 그 칸을 **분자에는 채운 것으로 넣고 분모에서는 빼고 있었다.**
그 결과 해석별 유효채움이 100%를 넘었다(구조·강성 106.5%, 워피지 107.4%,
전기·EMI 103.7%, 열전달 100.2%).

12차에 해석별 표를 고쳤는데 **13차에 요약줄에서 같은 버그가 하나 더 나왔다** — 남은 일이
**−309칸**으로 인쇄됐다. **지표를 고칠 때는 같은 수식이 쓰인 자리를 전부 찾아야 한다.**
현재 값은 유효채움 **99.5%**, 남은 일 **72칸**(376 − 304, 304 = 부재이면서 아직 비어 있는 칸)이다.

**(나) 문서화된 method×tier 조합표가 좁았다 — 표를 버리고 불변식 둘로 바꿨다.**
`PROPERTY_DATA_HISTORY.md`의 "정한 규칙" 2항이 허용 조합 7개를 표로 못박고
**"다른 조합이 나오면 어딘가 어긋난 것이다"**라고 적었다. 그런데 표 밖 조합이 225건이었다.
전수로 열어 보니 **대부분은 데이터가 아니라 표가 틀렸다.**

| 조합 | 건수 | 열어 본 결과 |
|---|---:|---|
| `computed` @ tier1 | 103 | **정당하다.** 벤더가 각주로 *"calculated value per datasheet footnote"*라 밝힌 값, 논문이 자기 시편에 피팅해 인쇄한 Prony 항 — 그 제품에 대해 인쇄됐으니 tier1이고, 계산값이니 computed다 |
| `computed` @ tier2 | 104 | **정당하다.** 인쇄된 두 수로 우리가 환산한 값을 한 단계 낮춘 것이다(tesa 61395: 20 N/cm ÷ 200 µm 두께 = 10 MPa). 기하가 개입하는 환산이라 tier1을 주지 않는 것이 보수적으로 옳다 |
| `measured` @ tier2 | 2 | **정당하다.** 다른 시료의 실측을 계열값으로 쓴 것이다 |
| `handbook` @ tier1 | 1 | 제조사(Special Metals) 자사 불리틴의 정성 등급이라 tier1이 맞다 |
| **`estimated` @ tier3** | **15** | **이것만 진짜 결함이었다.** 전부 CRC·ASM·벤더 TDS에 **인쇄된 계열값**인데 method가 estimated였다. 우리가 만든 수가 아니므로 `handbook` 14 · `measured` 1로 정정했다 |

**조합을 세는 것이 애초에 잘못된 검사 설계였다.** 조합표는 허용 목록이라 새로운 정당한
조합이 생길 때마다 오탐을 낸다. 뜻이 어긋나는 것만 잡으면 규칙이 둘로 줄어든다.

1. **`estimated`는 "우리가 만든 값"이다** → tier4가 아니면 어느 쪽이든 거짓말이다.
2. **tier4는 "우리가 만든 값"이다** → `measured`·`handbook`이면 등급이 틀렸다.

`integrity_check.py`가 이 둘을 검사한다(37항목). 앞서 지적한 `estimated` @ tier1 8건은
12차에 이미 `measured`로 정정됐다 — 전부 그 논문이 그 재료에 대해 식별한 모델 상수였다.

### 11.11 요지

이 절의 사례들에는 공통 구조가 있다.

1. **숫자가 이상하다는 감각에서 시작한다** — "천장이 100%일 리 없다", "0.0%는 이상하다".
2. **가설을 반증 가능한 형태로 만든다** — 부재를 (재료군 × 물성) 쌍으로 선언하고 반례를 센다.
3. **반례가 하나라도 나오면 선언이 무너지게 자동화한다** — 사람이 기억할 필요가 없다.
4. **그리고 무너진 기록을 남긴다** — 그래야 같은 가설을 다시 세우지 않는다.

**틀렸다는 것을 문서에 쓰는 것이 이 방법론의 산출물이다.**
카탈로그의 신뢰도는 "우리가 옳았다"에서 오지 않고
**"틀린 것을 어떻게 찾아냈는가"가 기록돼 있다는 데서 온다.**

---

## 부록 A — 재현 명령

```bash
# 현재 커버리지 (해석별 4지표 · 물성 보유율)
python3 backend/scripts/catalog/coverage_report.py
python3 backend/scripts/catalog/coverage_report.py --json   # 기계 판독용

# 무결성 37항목 — 0이 아니면 배포 금지
python3 backend/scripts/catalog/integrity_check.py

# 파동 인제스트
python3 backend/scripts/catalog/ingest_agent_json.py <산출 디렉터리>          # 드라이런
python3 backend/scripts/catalog/ingest_agent_json.py <산출 디렉터리> --apply
python3 backend/scripts/catalog/normalize_tiers.py --apply

# 문서 재생성
python3 backend/scripts/catalog/build_coverage_docx.py       # 커버리지 보고서
python3 backend/scripts/catalog/build_methodology_docx.py    # 이 문서의 docx
python3 backend/scripts/catalog/build_stats_xlsx.py          # 통계 워크북
```

**DB는 읽기만 한다.** 쓰는 것은 `ingest_agent_json.py --apply`와
`normalize_tiers.py --apply` 두 경로뿐이고, 둘 다 드라이런이 기본이다.

## 부록 B — 파일 지도

| 파일 | 역할 |
|---|---|
| `docs/COLLECTION_BRIEF.md` | **수집 지시서 정본.** 0절 원칙 · 0-A·0-B 방침 · 1절 tier · 2절 접근 경로 · 3절 출력 형식 · 5절 작업 습관 · **6절 함정 지도** |
| `docs/PROPERTY_DATA_HISTORY.md` | **경위의 1차 사료.** 72개 장. 파동별로 무엇을 시도했고 무엇이 틀렸는지 |
| `docs/BLOCKED_SOURCES.md` | 막힌 경로 대장. 유료(A)/로그인(B)/미공표(C)/삭제(D)/그림뿐(E) + **뚫렸던 경로** |
| `docs/FDE_FATIGUE_ARCHIVE.md` | 사이트를 잃고 Wayback에서 회수한 피로계수 26세트 |
| `docs/coverage-honest/` · `docs/hub-export/` | 결정 기록(계획·체크리스트·컨텍스트 노트) |
| `backend/app/property_taxonomy.py` | 물성 정의 161종 |
| `backend/scripts/catalog/coverage_report.py` | 지표 정의 · 구조적 부재 선언 · 반례 검증 |
| `backend/scripts/catalog/integrity_check.py` | 무결성 37항목 + 주의 1항목 |
| `backend/scripts/catalog/ingest_agent_json.py` | 적재 6단계 검증 · 조건 충돌 감지 |
| `backend/scripts/catalog/normalize_tiers.py` | tier를 "값의 성격"으로 재판정 |
